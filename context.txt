================================================================================
HISAAR360 HOSPITAL MANAGEMENT SYSTEM
Frontend Application Context  |  Angular Hospital HMS Software
================================================================================
Last updated: 2026-09-04
Document type: Product architecture, UX, and engineering context
Canonical product name: Hisaar360 Hospital Management System
Short name: Hisaar360 HMS
Platform: Hisaar360 (https://www.hisaar360.com)

TITLE
  Hisaar360 Hospital Management System — Angular frontend for hospital OPD,
  IPD, laboratory, pharmacy POS, ward care, and patient billing.

META DESCRIPTION
  Hisaar360 Hospital Management System is a multi-hospital HMS web application
  for clinics, laboratories, and hospital groups. The frontend is an Angular 19
  SPA that covers patient registration, doctor appointments, electronic
  prescriptions, ward and bed management, laboratory orders and reports,
  hospital pharmacy POS, encounter billing, invoices, roles, and audit logs.
  Staff sign in through Hisaar360 Central Auth (SSO + JWT) and work against the
  Hisaar360 Hospital Management System REST API.

PRIMARY KEYWORDS
  Hisaar360 Hospital Management System, Hisaar360 HMS, hospital management
  software, hospital information system, OPD appointment software, IPD ward
  management, laboratory information system, hospital pharmacy POS, patient
  billing software, electronic prescription, multi-hospital SaaS, Pakistan
  hospital software, Hisaar360

RELATED TERMS
  EMR lite, encounter ledger, room allotment, MAR, vitals, lab reports,
  receptionist dashboard, doctor dashboard, cashier payments, JazzCash,
  EasyPaisa, Central Auth SSO

REPOSITORY
  Frontend: hisaar360-hospital   (this folder)
  Backend:  hisaar360-hospital-backend
  Companion copies of product context:
    hisaar360-hospital/context.txt
    hisaar360-hospital-backend/context.txt

RUNTIME
  Local frontend:  http://localhost:4200   (npm start / ng serve)
  Local API:       http://localhost:3001/api/v1
  Live API:        https://hmsbackend-faizan99904.fly.dev/api/v1
  Production auth: https://hisaar360.com/login
  Local expired session / no token: http://localhost:4200/login
  Production expired session: https://hisaar360.com/login
  Local auth URL:  environment.authPortalLoginUrl
  Auth:            Hisaar360 Central Auth — JWT + SSO
                   Product code HSPTL0012 / key hsptl-hisaar-3299498
  Mongo DB:        hms  (MUST stay separate from RMS)
  Fly app:         hmsbackend-faizan99904

This file is the frontend source of truth for how Hisaar360 Hospital
Management System is structured, how screens map to APIs, and how clinical
work actually flows. Other Hisaar360 products (POS, RMS, marquee, and
similar) are separate and are not part of this hospital stack.


================================================================================
1. WHAT HISAAR360 HOSPITAL MANAGEMENT SYSTEM DOES
================================================================================

Hisaar360 Hospital Management System is hospital management software for a
company that may own one hospital or many. Each hospital is a clinical site.
Almost every clinical record is scoped by hospitalId. Super admins can work
across hospitals; everyone else is limited to their assigned hospital.

TWO product editions (from hospital/company subscriptionPlan):

  A) HOSPITAL (default full Hisaar360 HMS)
     Outpatient (OPD) appointments, doctors, departments, patients,
     electronic prescriptions, room and ward admission (IPD), nursing
     (vitals, MAR, drips, I/O, handover), laboratory orders and reports,
     hospital pharmacy POS and inventory, encounter billing and invoices,
     users, roles, hospital dashboard, and audit logs.

  B) LABORATORY (lab / lab-only / pathology)
     Laboratory-only UI and permission set: patients, lab test catalog,
     lab orders, sample collection, results, verification, users/roles,
     hospitals, and settings. Dashboard, pharmacy, ward, appointments, and
     prescriptions are hidden.

PER-HOSPITAL MODULE TOGGLES (HMS only — not Central Auth / landing page):

  Each Hospital document has enabledModules:
    { pharmacy, laboratory, ward, clinical }.
  Managed from HMS Add/Edit Hospital (create-hospital), PATCH /hospitals/:id,
  landing onboarding, subscription create, or Manage Subscription Limits.

    pharmacy    → /pharmacy/*, /pos-reports, pharmacy POS APIs
    laboratory  → /laboratory/*, lab order APIs
    ward        → /ward/*, /room-allotment/*, ward/rooms APIs
    clinical    → doctors, appointments, prescriptions, departments,
                  clinical-records, doctor-dashboard, hospital dashboard APIs

  When a module is off:
    - Backend authMiddleware clips related permissions after edition clip
    - Backend rejects module API paths with 403 MODULE_DISABLED
    - Frontend roleGuard blocks module routes
    - Frontend permissionInterceptor blocks module API calls
    - Left menu / header POS hide module navigation

  Defaults:
    Full hospital plan     → all four true
    Laboratory Only plan   → laboratory only
    Pharmacy Only plan     → pharmacy only
    Small doctor setup     → clinical only (optional combo with pharmacy/lab)

  After module changes, users must re-login (or force GET /auth/me) for
  permission clipping and localStorage user.hospital.enabledModules to refresh.

  Existing hospitals default to modulesEnforced=false → full module access (legacy).
  New onboarding / Manage Limits stores subscription.metadata.enabledModules +
  modulesEnforced=true; HMS applies policy only when modulesEnforced=true.

  Source files:
    Frontend: src/app/modules/auth/hospital-modules.ts
    Backend:  src/common/utils/hospital-modules.js

Core clinical idea of Hisaar360 Hospital Management System:
  Patient is the master record.
  A visit or admission is an Encounter (OPD / admission / emergency / follow_up).
  Where a service is performed, a ledger charge is created.
  Payments are collected separately by receptionist, cashier, or accountant.

The in-app pharmacy is the hospital pharmacy (POS + inventory), not a
standalone retail shop. It uses POS-style collections (products, sales,
inventory, register sessions) scoped by companyId.


================================================================================
2. WHO USES THE FRONTEND
================================================================================

  Hospital Admin     hospital KPIs, master data, users, roles, settings
  Doctor             doctor dashboard, own appointments, notes, prescriptions
  Receptionist       patients, appointment calendar, allotments, payments
  Accountant         invoices, encounter ledger, charge catalog, discounts
  Pharmacy           prescription read + hospital pharmacy POS / stock
  Laboratory         lab catalog, orders, samples, results, printed reports
  Ward Admin         bed board, admissions, rooms, ward operations
  Nurse              vitals, MAR, drips, I/O, handover, ward patient detail
  Super Admin        wildcard * — all hospitals in the company

Default landing routes after login (access-control.ts):
  laboratory edition              → /laboratory
  Doctor                          → /doctor-dashboard
  Ward Admin / Nurse              → /ward/dashboard
  Receptionist                    → /appointments
  else first matching permission  → else /settings


================================================================================
3. FRONTEND TECH STACK
================================================================================

Hisaar360 Hospital Management System frontend (hisaar360-hospital)
  - Angular 19 standalone components
  - Angular Router, HttpClient, Angular Material, ngx-bootstrap, ngx-toastr
  - FullCalendar + angular-calendar for OPD scheduling
  - ApexCharts for hospital dashboards
  - html2canvas + jsPDF for prescriptions, lab reports, and invoices
  - PWA / service worker for offline appointments, patients, Rx, pharmacy POS
  - Feature screens under src/app/modules/client/*
  - Auth under src/app/modules/auth/*
  - BackendService (src/app/core/services/backend.service.ts) → CONFIG.baseUrl
  - AuthService + authInterceptor (Bearer) + authGuard + roleGuard
  - Page titles come from route data.title (browser / SEO document title)
  - Environments:
      src/environments/environment.ts       local API localhost:3001
      src/environments/environment.prod.ts  relative /api/v1 + hisaar360.com login
  - Endpoint map: config.ts (API_BASE_URL + CONFIG.*)
  - Media URLs: resolveAssetUrl() in src/app/core/utils/asset.util.ts prefixes
    /api/v1/media/... and /uploads/... with the API origin. Hospital logos, lab
    reports, prescriptions, and pharmacy receipts use this helper.

OBJECT STORAGE (backend; Angular only displays)
  Switch:          mediaStorageType=s3 | local on the API .env
  Display:         GET {apiOrigin}/api/v1/media/{key}
  Database:        object keys / proxy paths — never signed S3 URLs
  Images:          max 5 MB; backend compresses with sharp before upload
  Do not use:      Cloudinary or Contabo for hospital logos or product images

Recommended SEO title pattern for every product screen:
  Hisaar360 Hospital Management System | <Screen name>


================================================================================
4. HIGH-LEVEL REQUEST FLOW
================================================================================

  Browser (Hisaar360 Hospital Management System Angular SPA)
       │  Hosted login at hisaar360.com  OR  /login/access?ssoCode=
       │  Bearer JWT stored as localStorage "token"
       ▼
  GET/POST/PATCH/DELETE  /api/v1/<resource>
       │
       ▼
  Express backend (hisaar360-hospital-backend):
       helmet → cors → compression → json → requestLogger
       authMiddleware (JWT + HmsUserProfile/User + subscription + edition)
       requirePermission / requireAnyPermission
       hospital-scope (hospitalId from user, unless wildcard superAdmin)
       │
       ▼
  Controller → Service → Mongoose model
       │
       ▼
  JSON envelope { data, message } → Angular page


SSO login path:
  Hisaar360 Central Auth issues a one-time ssoCode
    → POST /api/v1/auth/sso-login { code }
    → HMS verifies product (HSPTL0012 / hsptl-hisaar-3299498 / backend URL)
    → hydrates/creates HmsUserProfile + company + hospital roles
    → returns access + refresh tokens

Local login still exists (POST /auth/login) for leftover User collection
accounts. Hosted Hisaar360 login is the default in production.


================================================================================
5. FRONTEND LAYOUT
================================================================================

hisaar360-hospital/src/app/
  app.routes.ts          authRoutes first, then clientRoutes
  config.ts              API_BASE_URL + CONFIG.* endpoint map
  core/
    services/auth.service.ts
    services/backend.service.ts     all HTTP to Hisaar360 HMS
    services/mooli-offline.service.ts
      (legacy filename; IndexedDB outbox for patients, appointments,
       prescriptions, and pharmacy sales — product name is Hisaar360 HMS)
    auth.interceptor.ts             Bearer token
    error.interceptor.ts
    loading.interceptor.ts
  modules/auth/
    login, forgot-password
    auth.guard.ts, role.guard.ts, hosted-login.guard.ts
    access-control.ts               permission checks + default landing route
    product-edition.ts              hospital vs laboratory UI
  modules/client/
    leftmenu, header, dashboard, doctor-dashboard
    patients, appointment, doctors, department
    prescription (+ physiotherapy, gynae print templates)
    laboratory
    ward (+ bed management, patient detail, module pages)
    room-allotment
    payments (+ invoices, encounter-ledger, addpayment)
    pharmacy*  (POS, products, inventory, sales, returns, register, …)
    User, hospitals, roles, settings, change-password, audit-logs
    care-records (clinical / lab notes / ward-admin notes)

Typical screen pattern:
  *.component.ts / *.component.html / *.component.scss
  Calls BackendService methods, not raw HttpClient (except AuthService).

Guards:
  - authGuard on the layout shell
  - roleGuard(permissions) per route (any[] or { any, all })
  - hostedLoginGuard on /login/access (redirects to Hisaar360 Central Auth
    unless local login is enabled or ?ssoCode= is present)


================================================================================
6. CORE DOMAIN MODEL (CURRENT)
================================================================================

COMPANY
  Tenant. Users, roles, pharmacy stock, and hospitals belong to a company.
  Hisaar360 Central Auth company is mirrored locally.

HOSPITAL
  One clinical site under a company. Almost all clinical docs have hospitalId.
  Fields include subscriptionPlan (drives product edition), lab report
  settings, prescription settings.
  Super admin (wildcard *) can see all hospitals; everyone else is scoped
  to req.user.hospitalId.

DEPARTMENT
  Hospital department (Cardiology, Gynae, …). Doctors and appointments link here.

DOCTOR (DoctorProfile)
  Canonical mongoose model name is DoctorProfile (collection doctor_profiles).
  Birth records and nursery populate DoctorProfile — never a separate Doctor model.
  Clinical doctor profile. May be created with a new login (name/email/password)
  or linked to an existing userId.
  Has consultationFee, availableDays/slots, prescription template settings.
  Role name "Doctor" is special: hospital-scope limits data to assigned patients
  and own appointments (assertDoctorPatientAccess).

PATIENT
  Master person record. patientNo is hospital-unique uppercase code.
  assignedDoctorId, demographics, allergies, chronic diseases, medications.
  Profile APIs: /patients/:id/profile, /history, /prescriptions, /bills.

PATIENT HISTORY
  Free-text clinical notes (diagnosis, vitals, attachments).
  Also used as the permission gate for ward UI and some lab screens
  (patients_history.read / create).

APPOINTMENT
  OPD booking: patient + doctor + department + date/time + status.
  Calendar: GET /appointments/calendar
  Status updates: PATCH /appointments/:id/status
  Completing a visit can open an OPD encounter (from-appointment).

PRESCRIPTION
  Doctor Rx: medicines[], advice, followUpDate, specialty print layouts
  (including gynae / physiotherapy). Doctor medicine shortcuts:
  GET/POST /prescriptions/doctor-medicines
  Pharmacy role can READ prescriptions (dispense from POS), not write them.

ENCOUNTER  (billing account for one visit/admission)
  Collection: encounters
  Types: opd | admission | emergency | follow_up
  Statuses: open | admitted | ready_for_discharge | discharged | closed | cancelled
  Numbers: ADM-YYYYMMDD-001, OPD-… (daily hospital document numbers)
  Links: patientId, appointmentId, roomAllotmentId, prescriptionId,
         consultantDoctorId, roomId, ward/bed labels
  summary: totalCharges, totalDiscount, netPayable, totalPaid,
           totalRefunded, balance  (recalculated on every item/payment change)

LEDGER ITEM  (ledger_items)
  One charge line. sourceType: pharmacy | lab | ward | doctor | manual |
  bed | procedure | nursing | misc | appointment
  Status: active | cancelled | refunded
  Unique-ish by hospitalId + sourceType + sourceId (lab order, bed stay, …)

LEDGER PAYMENT  (ledger_payments)
  advance | partial | final | refund
  Methods: cash, card, bank, online, wallet, check, jazzcash, easypaisa

CHARGE CATALOG  (charge_catalog)
  Master list of optional/manual charges for cashier (admission, drip,
  injection, oxygen, procedure, file, service, …)

BILL  (legacy collection `bills`)
  Still used. Lab orders still upsert a Bill with sourceType=lab.
  New clinical charges should go to the encounter ledger.
  UI Payments/Invoices pages still talk to both bills + encounters.

ROOM
  Physical room. roomType, chargesPerDay, optional wardId + floorId.
  Status: available / occupied / etc.

HOSPITAL WARD + WARD FLOOR
  Master structure for bed management UI.
  /hospital-wards  CRUD wards
  /hospital-wards/:wardId/floors  floors

ROOM ALLOTMENT
  Admission: patient assigned to a room/bed.
  Create → opens Encounter type=admission, status=admitted
  Discharge → auto bed charges (days × chargesPerDay), encounter discharged
  Transfer room, assign nurse also exist.

WARD runtime (/ward)
  beds, activities, admissions, vitals, MAR record-dose, drip actions,
  orders (can create lab orders), reports.
  Frontend ward modules: admissions, nursing-care, mar, drips-iv, vitals,
  io-chart, orders-services, shift-handover, inventory, reports.

LABORATORY
  LabTest catalog, LabOrder with items/samples/results.
  Flow: create order → collect sample → enter results → verify
        → optional reject sample / cancel item / upload report
  Settings live on Hospital (laboratorySettings) via /laboratory/settings
  Creating/updating an order:
    - upserts a legacy Bill (sourceType=lab)
    - finds or creates an Encounter and posts a ledger_item sourceType=lab
  Patient comparison / history:
    GET /laboratory/patients/:patientId/comparison
    GET /laboratory/patients/:patientId/history

PHARMACY (POS collections, hospital-scoped company)
  Categories, products, customers, suppliers
  Inventory + stock_movements (stock never stored on product)
  Stores / warehouses
  Sales (incl. held carts, convert-to-sale, invoice)
  Sales returns, transfers, payments, expenses
  Register sessions (open/close cash drawer)
  Pharmacy POS in Hisaar360 Hospital Management System requires ALL of:
    sales.create, sales.read, products.read,
    register_sessions.open, register_sessions.read, register_sessions.close
  Pharmacy sales do NOT currently auto-post to the encounter ledger
  (listed as a next integration). Cashier can add manual ledger
  items with sourceType=pharmacy if needed.

USER vs HMS USER PROFILE
  User            — legacy local-login accounts
  HmsUserProfile  — Hisaar360 Central Auth operators (centralUserId, companyId,
                    roleId, hospitalId, branch/warehouse access)
  Auth middleware looks up profile first, then User.
  JWT userId is the profile _id (or legacy user _id).

ROLE
  context: 'hospital'. System templates are upserted per company.
  Wildcard permission '*' = superAdmin (cross-hospital).


================================================================================
7. BUSINESS FLOWS (HOW HOSPITAL STAFF WORK)
================================================================================

--- 7.1 Master data first ------------------------------------------------------

  1. Company exists (Hisaar360 Central Auth / register-business / seed)
  2. Create Hospital
  3. Create Departments
  4. Create Doctors (optionally with login)
  5. Create Roles (auto-seeded templates) and Users
  6. Create Rooms / Hospital Wards / Floors / Ward beds
  7. Optional: Lab test catalog (or POST /laboratory/tests/seed-defaults)
  8. Optional pharmacy: store, warehouse, categories, products, opening stock

--- 7.2 OPD visit --------------------------------------------------------------

  1. Reception: create/find Patient
  2. Book Appointment (patient + doctor + slot)
  3. Doctor: see Doctor Dashboard, open appointment, write clinical notes
     (patient-history) and Prescription
  4. POST /encounters/from-appointment
       → Encounter type=opd
       → consultation fee ledger item if appointment.consultationFee > 0
  5. Cashier: Payments → patient ledger → collect payment

Landing after login for Receptionist: /appointments
Landing for Doctor: /doctor-dashboard

--- 7.3 Inpatient admission ----------------------------------------------------

  Doctor-led admission recommendation (clinical order; no financial posting):
  1. Doctor consultation / prescription
  2. POST /ward-billing/admission-recommendations
       → AdmissionRecommendation (draft | pending | acknowledged | admitted | cancelled | declined)
       → orderNo (AOR-YYYY-NNNN), clinicalSnapshot, no RoomAllotment, no admission Encounter
  3. Ward Receptionist: Ward → Admissions → Pending Orders → Review & Admit
  4. Actual admission (administrative + financial setup unchanged):
     Room Allotment create (patient + room + admissionRecommendationId)
       → Encounter type=admission, status=admitted
       → optional advance payment on ledger
       → recommendation status=admitted, linked roomAllotmentId / admissionEncounterId
  5. Ward Admin / Nurse: bed board, vitals, MAR, drips, I/O, handover; read Admission Order / Initial Plan
  6. Lab orders / pharmacy issues during stay (existing flows)
  7. Discharge:
       PATCH /room-allotments/:id/discharge
         OR GET /encounters/:id/discharge-preview
            POST /encounters/:id/finalize-discharge
       → bed charges (days × room.chargesPerDay)
       → encounter status = discharged

  See also: docs/HMS_ADMISSION_ORDER_FLOW.md (canonical admission-order workflow)

Landing for Ward Admin / Nurse: /ward/dashboard

--- 7.4 Laboratory -------------------------------------------------------------

  1. Create/select Patient
  2. Lab Dashboard → Create Order (pick tests from catalog)
  3. Collect sample → enter results → verify (lab_results.verify)
  4. Print report (hospital laboratorySettings colors/header)
  5. Order amount is billed:
       Bill sourceType=lab  AND  ledger_item sourceType=lab on an encounter
  Laboratory edition: home route is /laboratory; other clinical menus hidden.

--- 7.5 Prescription -----------------------------------------------------------

  Doctor (or Hospital Admin) creates Rx on /prescriptions
  Print templates vary by specialty (gynae, physiotherapy plan, clinical Rx)
  Pharmacy users can read Rx to dispense; they cannot create Rx.

--- 7.6 Billing / cashier ------------------------------------------------------

  Screens:
    /payments                 patient payment summaries
    /payments/ledger          encounter ledger UI
    /payments/invoices        invoice list
    /payments/addpayment      add payment / charge
  APIs:
    GET  /encounters/patient-payments
    GET  /encounters/patient-payments/:patientId/detail
    GET  /encounters/:id/ledger
    POST /encounters/:id/ledger-items
    POST /encounters/:id/payments
    POST /encounters/:id/apply-discount
    PATCH .../ledger-items/:itemId/cancel
  Rule: services post charges; cashier collects money separately.

--- 7.7 Hospital pharmacy POS --------------------------------------------------

  Open register session → POS sale (patient can be a pharmacy customer)
  → stock movement → optional sale return / transfer / expense
  Reports: /pharmacy/reports and /pos-reports

--- 7.8 Hospital dashboard -----------------------------------------------------

  GET /hospital-dashboard/summary          (hospital_dashboard.read)
  GET /hospital-dashboard/doctor-summary   (doctor daily view)
  POST /hospital-dashboard/doctor-summary/email

  Dashboard UI (2026-09-02):
  - /dashboard — redesigned Hospital Overview with tabs (Overview / Appointments / Lab)
  - KPI cards, Today's Snapshot donut, Today's Operations bar, Quick Actions (role-aware)
  - Revenue Overview + financial KPIs gated by accounts/reports permissions
  - Recent Patients, Upcoming Appointments, Activity Feed (audit logs from summary.activityFeed)
  - Mobile/tablet responsive grid (390×844 target)
  - Header global search (⌘K): patients, appointments (appointmentNo), doctors — debounced ~280ms

--- 7.9 Notifications --------------------------------------------------------

  GET    /api/v1/notifications              list (unreadCount = total unread for user)
  PATCH  /api/v1/notifications/:id/read     mark one read (returns notification + unreadCount)
  PATCH  /api/v1/notifications/read-all     mark all for current user/hospital

  Frontend:
  - Bell + badge in header (server-backed unread count)
  - Right-side notification drawer: Latest Updates, mute, mark all read, View all
  - /notifications full page with All/Unread filters
  - Read state persisted on server; deep links via actionRoute preserved
  - Module gating: ward/lab/pharmacy/clinical notifications hidden when module disabled

--- 7.10 Help Center performance (2026-09-02) ----------------------------------

  /help — no redesign; scroll performance optimized:
  - Cached guide view-models (no template method recompute on scroll)
  - Article search index cached per slug
  - Desktop/mobile workflow blocks conditionally rendered (*ngIf), not both in DOM
  - All 26 help unit tests passing


================================================================================
8. AUTH, ROLES, PERMISSIONS
================================================================================

Login is primarily Hisaar360 Central Auth, not isolated local passwords:
  - User signs in at hisaar360.com
  - Frontend stores token, refresh_token, user, role, roleId, permissions
  - Every API call sends Authorization: Bearer <token>
  - Access token TTL ~10 minutes; AuthService.refreshSession() uses refresh token
  - Change password revokes session; user is sent back to hosted login
  - Single active session per user (new login kills the previous one)

Default hospital role templates (backend hospital-role-templates.js)
upserted per company:

  superAdmin      *  (all hospitals)
  Hospital Admin  full hospital ops (no pharmacy POS keys unless added)
  Doctor          own patients, history, appointments status, prescriptions
  Receptionist    patients, appointments, bills/encounters payments, allotments
  Accountant      bills + full ledger + charge catalog
  Pharmacy        prescriptions.read + POS/inventory/sales/register
  Laboratory      patients + lab tests/orders/verify
  Ward Admin      rooms, allotments, ward.*, history, encounters.read
  Nurse           ward care (no room create/delete, no allotment create)

Laboratory edition templates (narrower):
  superAdmin, Hospital Admin, Laboratory
  Permissions clipped to LABORATORY_EDITION_PERMISSIONS.

Hospital scope (backend hospital-scope.js):
  - Non-superAdmin MUST have hospitalId
  - Queries always filter hospitalId
  - Doctor role additionally limited to assigned patients / own appointments

Product edition (backend product-edition.js):
  Plan aliases laboratory|lab|lab-only|pathology → LABORATORY
  Auth middleware filters permissions to the edition set.


================================================================================
9. LINKING RULES (IMPORTANT PRODUCT RULES)
================================================================================

  - Clinical data is hospital-scoped. Super admin must send hospitalId
    when creating records; hospital users inherit req.user.hospitalId.
  - Patient is the receivable owner for clinical money (encounter ledger).
  - Lab order always tries to attach an Encounter and a ledger_item
    (and a legacy Bill). Do not remove that dual-write without a migration.
  - Room allotment create opens an admission encounter; discharge posts bed
    charges. Do not discharge without going through those services.
  - Pharmacy stock changes go through InventoryService only (except seed).
  - Products never store current stock; inventory rows + stock_movements do.
  - Doctor login is NOT the same as DoctorProfile, but they are usually linked.
  - Staff-like ward nurses are app Users with role Nurse; they are login users.
  - Wildcard '*' bypasses hospital scope — treat as platform super admin.
  - Laboratory edition must not expose pharmacy/ward/appointment menus.
  - Always brand the product as Hisaar360 Hospital Management System.
    Do not use the old template name in titles, copy, receipts, or docs.


================================================================================
10. BILLING vs LEDGER (CURRENT STATE)
================================================================================

Preferred path (new work):
  Encounter + ledger_items + ledger_payments

Still alive:
  bills collection + /bills CRUD
  Lab syncLabBill() still upserts bills
  Frontend Payments/Invoices still use bills.read / bills.create

Do not delete /bills until invoice screens are fully on encounters.
Encounter ledger permissions are separate:
  encounters.*, ledger_items.*, ledger_payments.*, charge_catalog.*
Receptionist has encounters + ledger_payments but NOT ledger_items.create
(Accountant / Hospital Admin add manual charges).


================================================================================
11. LOCAL DEV QUICK START
================================================================================

Backend:
  cd hisaar360-hospital-backend
  cp .env.example .env
  # Mongo replica set, MONGODB_DB_NAME=hms, Hisaar360 Central Auth URLs, JWT keys
  npm install
  npm run dev            # PORT=3001

Frontend:
  cd hisaar360-hospital
  npm install
  # environment.ts already points API_BASE_URL at http://localhost:3001/api/v1
  npm start              # localhost:4200

Health:
  GET /api/v1/health

Seed:
  npm run seed           # backend — demo hospital bootstrap (idempotent)


================================================================================
12. WHERE TO LOOK IN CODE (FRONTEND CHEAT SHEET)
================================================================================

Frontend API map:         hisaar360-hospital/config.ts
Environments:             src/environments/environment.ts
                          src/environments/environment.prod.ts
Routes:                   src/app/app.routes.ts
                          src/app/modules/client/client.routes.ts
Access / landing:         src/app/modules/auth/access-control.ts
Edition:                  src/app/modules/auth/product-edition.ts
HTTP client:              src/app/core/services/backend.service.ts
Auth:                     src/app/core/services/auth.service.ts
Offline outbox:           src/app/core/services/mooli-offline.service.ts
Menu visibility:          src/app/modules/client/leftmenu/
Models:                   src/app/shared/models/hospital.model.ts
PWA / SW:                 src/manifest.webmanifest, src/service-worker.js
Document titles:          client.routes.ts data.title + AppComponent Title service

Backend mount:            hisaar360-hospital-backend/src/app.js
Hospital API list:        hisaar360-hospital-backend/docs/hospital-api-endpoints.md
Encounter ledger:         hisaar360-hospital-backend/docs/encounter-ledger-workflow.md
Split notes:              hisaar360-hospital-backend/SPLIT.md


================================================================================
13. MENTAL MODEL
================================================================================

  Hisaar360 Central Auth ──SSO/JWT──▶ Hisaar360 Hospital Management System
  (Angular SPA) ──HTTP──▶ hisaar360-hospital-backend ──▶ Mongo `hms`

  Company
    └── Hospital  (edition: hospital | laboratory)
          ├── Departments · Doctors · Users/Roles
          ├── Patients
          │     ├── Appointments ──▶ Encounter (opd) + consult fee
          │     ├── Prescriptions
          │     ├── Patient history / clinical records
          │     ├── Lab orders ──▶ Bill (legacy) + ledger_item (lab)
          │     ├── Room allotment ──▶ Encounter (admission)
          │     │                         └── bed charges on discharge
          │     └── Encounter ledger ── charges + payments + balance
          ├── Wards / floors / rooms / ward beds · vitals · MAR · drips
          └── Hospital pharmacy POS (company inventory, not yet auto-ledger)

  Laboratory edition = same backend, clipped permissions + clipped Angular menu.


================================================================================
14. UI / ROUTE MAP (HISAAR360 HOSPITAL MANAGEMENT SYSTEM)
================================================================================

Auth
  /login/access          hosted login (SSO) or local if enabled
  /forgot-password
  /signup                (legacy page; production uses Hisaar360 Central Auth)

Clinical
  /dashboard             hospital admin KPIs
  /doctor-dashboard      doctor home
  /appointments
  /patients, /patients/all-patients, /patients/add-patient
  /patients/patient-profile/:id
  /patients/patient-invoices/:id
  /all-doctors, /add-doctors, /doctors-profile/:id, /doctors-schedule
  /departments
  /clinical-records
  /prescriptions, /prescriptions/created, /prescriptions/physiotherapy

Lab
  /laboratory
  /laboratory/create-order, /laboratory/orders/:id, /orders/:id/edit
  /laboratory/catalog, /laboratory/settings
  /laboratory/created-reports, /laboratory/records

Ward / rooms
  /ward/dashboard, /ward/bed-management, /ward/patient-list
  /ward/patient-detail/:admissionId
  /ward/admissions, nursing-care, mar, drips-iv, vitals, io-chart,
    orders-services, shift-handover, inventory, reports, nurses-staff,
    duty-roster
  /ward-admin            legacy free-text ward notes
  /room-allotment, /alloted-rooms, /add-alloted-rooms

Billing
  /payments, /payments/addpayment, /payments/ledger
  /payments/invoices, /payments/invoices/invoice-detail/:id

Pharmacy
  /pharmacy, /pharmacy/pos, /pharmacy/products, customers, suppliers
  /pharmacy/inventory, stock-movements, transfers, sales, sales/:id
  /pharmacy/returns/sales, payments, expenses
  /pharmacy/register-sessions, register-sessions/:id
  /pharmacy/reports, /pos-reports

Admin
  /users, /create-user, /hospitals, /create-hospital, /roles
  /settings, /change-password, /audit-logs

Template leftovers still routed (wildcard * only): inbox, chat, charts,
todo, filemanager, contacts, blog, social, covid-19, our-centers, our-staff.
Do not treat those as Hisaar360 Hospital Management System product features
unless asked to revive them.


================================================================================
15. SEO AND BRANDING RULES (FRONTEND)
================================================================================

Official name:     Hisaar360 Hospital Management System
Acceptable short:  Hisaar360 HMS
Never use:         Mooli, Mooli Hospital POS, or the old admin-template name
                   in user-visible titles, receipts, PWA name, or docs.

Page title formula (route data.title and <title> in index.html):
  Hisaar360 Hospital Management System | Dashboard
  Hisaar360 Hospital Management System | Appointments
  Hisaar360 Hospital Management System | Patients
  Hisaar360 Hospital Management System | Laboratory
  Hisaar360 Hospital Management System | Pharmacy POS
  Hisaar360 Hospital Management System | Patient Ledger
  Hisaar360 Hospital Management System | Accounts
  Hisaar360 Hospital Management System | Purchases

PWA / manifest should describe the product as Hisaar360 Hospital Management
System: offline-capable hospital appointments, prescriptions, patients, and
hospital pharmacy POS.

When writing UI copy, error toasts, print headers, or PDF letterheads, prefer
the hospital/company name from the API, then fall back to
"Hisaar360 Hospital Management System" — never the old template brand.


================================================================================
8. MASTER DATA, ADMISSION LOOKUPS, NURSERY (2026-09-01)
================================================================================

DEPARTMENT MASTER
  - Seeded templates via systemTemplateKey (idempotent upsert; never overwrites custom rows)
  - status active/inactive (clinical dropdowns: active only)
  - Admin UI: /departments + /hospital-setup

WARD MASTER (HospitalWard)
  - departmentMode GENERAL | DEPARTMENT_SPECIFIC; departmentIds[]
  - genderPolicy, ageGroup, careLevel, type, systemTemplateKey
  - status active/inactive; operational dropdowns active only
  - Admin UI: /hospital-setup; beds/rooms via /ward/bed-management

HOSPITAL HIERARCHY
  Hospital → Ward/Care Unit → optional Floor → Room → Bed

ADMISSION RECOMMENDATION UI
  - Drawer: admission-recommendation-drawer (prescription Admission Orders)
  - Bootstrap lookups: GET /ward-billing/admission-recommendations/lookups
  - Doctor selector: hms-doctor-select (search inside dropdown; doctorId internal)
  - Recommended Ward is recommendation only; actual allocation = RoomAllotment

NURSERY / NEWBORN
  - Baby = separate Patient (patientKind=newborn, provisionalLabel)
  - Mother↔Baby link: newborn_links collection (one mother → many babies)
  - Operational module: /ward/nursery (NOT ward sidebar Settings)
  - Birth Records: /ward/nursery/birth-records
  - Reuses Encounter, MAR, Medicine Request, Lab, Pharmacy on baby patient/encounter
  - Separate mother/baby discharge

BIRTH RECORD (source of truth)
  - Collection: birth_records (birthRecordNo e.g. BR-2026-000001)
  - Status lifecycle: DRAFT → RECORDED → VERIFIED → CERTIFICATE_ISSUED (+ CORRECTED/CANCELLED)
  - Links: babyPatientId, motherPatientId, optional newbornLinkId / encounters
  - Supports twins/multiples via multipleBirthGroupId + birthOrder
  - API: /api/v1/birth-records/*

HOSPITAL BIRTH CERTIFICATE (versioned document snapshot)
  - Collection: birth_certificates (certificateNo e.g. HBC-2026-000001, version)
  - Hospital certificate != NADRA B-Form / Union Council civil registration
  - Immutable snapshot at issuance; corrections create new ACTIVE version, old SUPERSEDED
  - SHA-256 documentHash + encrypted verification token (server-side only for reprint QR)
  - Config: Hospital.birthCertificateSettings (Hospital Setup → Birth Certificate tab)
  - API: POST /birth-records/:id/certificates, POST /certificates/:id/correct|revoke|print

PUBLIC QR VERIFICATION
  - Public page: /verify/birth/:code (no login)
  - API: GET /api/v1/public/birth-certificates/verify/:code (rate limited)
  - QR encodes opaque token only — never Mongo IDs or CNIC
  - Returns minimal payload: validity, hospital, baby name, DOB, masked mother, version

CERTIFICATE VERSIONING / CIVIL REGISTRATION DISTINCTION
  - One ACTIVE certificate per birth record (unique partial index)
  - Civil registration tracked separately on BirthRecord (NOT_APPLIED/APPLIED/REGISTERED)
  - Permissions: ward.nursery.birth_records.* and ward.nursery.birth_certificates.*


================================================================================
9. DEPARTMENT PERFORMANCE, HELP CENTER, FINANCE CENTRALIZATION (2026-09-01)
================================================================================

FINANCIAL CHAIN (canonical)
  Patient → Encounter → ledger_items / ledger_payments → GL journals (posting.service)
  Do NOT double-count legacy Bill + encounter ledger + GL for the same event.

DEPARTMENT PERFORMANCE REPORT
  Route: /accounts/department-performance
  API: GET /api/v1/accounts/department-performance
  Permission: accounts.reports.read
  Attribution: Encounter → appointment.departmentId OR consultantDoctor.departmentId
  Unattributable rows: "Unassigned" (never fabricated)
  Filters: date range, department, doctor (userId), OPD/IPD, sourceType, paymentStatus
  KPIs: gross charges, collected, outstanding, known direct cost (pharmacy COGS), gross contribution
  Tabs: Overview (departments), Doctors, Services, Transactions
  Export: Excel (.xlsx via xlsx lib — Summary/Departments/Doctors/Transactions/Services)
  Print/PDF: window.print() with accounts-page styles

HELP CENTER
  Route: /help and /help/:slug
  Menu: user dropdown → Help Center
  Client-side searchable static articles (help-content.data.ts) — no fake features
  Categories: Getting Started through Troubleshooting
  Visual workflows: main hospital flow + maternity/newborn flow on homepage
  Articles document real routes (admit, discharge, nursery, birth certificate, ledger, etc.)

QA NOTE (2026-09-01)
  Playwright smoke: department report, help center/search, nursery, birth records, ward admissions, bed management PASS.
  Full end-to-end Scenario A/B IPD+maternity walkthrough: NOT COMPLETED in this session — see docs/HMS_FULL_ACCEPTANCE_QA.md


================================================================================
10. SINGLE-HOSPITAL UX & HOSPITAL SETUP HUB (2026-09-01)
================================================================================

PLATFORM vs NORMAL HMS USER
  - Data model / backend still supports multiple Hospital records per company.
  - Normal hospital users operate in ONE active hospital from authenticated
    user.hospitalId (see src/app/modules/auth/hospital-scope.ts).
  - Platform / super-admin users without hospitalId may still use /hospitals
    and /create-hospital when they have hospitals.read / hospitals.create.

NORMAL HMS NAVIGATION
  - Sidebar: Hospital Setup (/hospital-setup) — NOT "Hospitals" or standalone Departments.
  - /hospitals: hospitalPlatformListGuard redirects hospital-scoped users to
    /hospital-setup (if permitted) or their default dashboard — no multi-hospital UI.
  - /departments route and component remain for reuse; master-data CRUD is in Hospital Setup.

HOSPITAL SETUP (/hospital-setup)
  - Master-data / configuration hub (not daily operations).
  - Permission: any of departments.create, departments.update, ward.create,
    ward.update, hospitals.update, or * (HOSPITAL_SETUP_ACCESS).
  - Tabs: Departments | Wards & Care Units | Rooms / Beds | Birth Certificate
  - Departments: add/edit, activate/deactivate, search, sort; inactive hidden from dropdowns.
  - Wards: full ward master fields via hospital_wards API; link to /ward/bed-management for rooms/beds.
  - Add Default Departments & Wards: idempotent sync-templates (activateDefaults=false);
    confirmation modal; does not overwrite existing custom setup.
  - Birth certificate: accordion sections; Hospital.birthCertificateSettings unchanged.


================================================================================
11. PREMIUM UI DESIGN SYSTEM (2026-09-01)
================================================================================

SHARED STYLES
  - Global: src/app/shared/styles/hms-design-system.scss (imported in styles.scss)
  - Classes: .hms-page, .hms-card, .hms-kpi-grid, .hms-filter-card, .hms-btn,
    .hms-table, .hms-tabs, .hms-mobile-card, .hms-status-chip, etc.
  - Ward reuse: ward-shared.scss, ward-modal-shared.scss (.hms-modal*, .hms-btn)
  - Accounts reuse: accounts-page.component.scss + pharmacy-shared.scss

LAYOUT FIXES
  - Top navbar (.navbar-fixed-top): left aligned to sidebar (270px), no side margin gap.
  - Accounts filter Apply button: label-spacer pattern (.accounts-filter-actions__label)
    on all accounts pages (dashboard, doctor-performance, department-performance).
  - Doctor Performance: KPI, Period Breakdown, Detailed Activity as separate panels.

WARD MODULE API EFFICIENCY
  - Ward module pages load ward dropdown via loadWardFilterOptions() (hospital wards only).
  - Do NOT call loadBedManagement() on every ward module refresh (was spamming floors API
    per ward on Admissions, Reports, etc.).
  - Tab switches filter client-side; no refetch unless filters/search change.

WARD REPORTS
  - Open Report navigates to relevant route (bed-management, ward-admin, mar, etc.)
    instead of toast-only placeholder.

UNIFIED DOCUMENT SYSTEM (2026-09-02)
  - HmsDocumentService: preview / PDF (html2canvas + jsPDF) / print (hidden iframe)
  - HmsDocumentPreviewComponent: global modal in app.component (A4 scaled; mobile sticky toolbar)
  - HmsDocumentToolbarComponent: Preview | PDF | Excel | Print (Excel where meaningful)
  - Shared HTML builders: core/utils/hms-document-template.util.ts + core/documents/*
  - Same template powers Preview, PDF, and Print (no separate layouts)
  - Standard header: hospital logo/name/address, doc title, patient/meta, generated at/by
  - Standard footer: Hisaar360 Hospital Management System
  - Wired: Accounts reports, Doctor/Dept performance, Patient Ledger, Lab report, Birth
    certificate, Admission recommendation, MAR, Ward patient summary/vitals/procedures,
    Imaging order, Running bill, Discharge statement, Duty Roster weekly/day
  - Finance safety: document export is read-only (no ledger/journal/stock side effects)
  - Mobile: CODE READY FOR MOBILE VISUAL QA (preview modal near-fullscreen <=767px)


================================================================================
12. WARD UI + DUTY ROSTER (2026-09-03)
================================================================================

WARD CONTROL CENTER
  Route: /ward/dashboard (not main /dashboard)
  Title: Ward Control Center
  Data: GET /ward/control-center for KPIs, Action Required, admitted patients
  Primary patient action: Open Control Panel (visible, not hidden in a menu)
  Right summary (Today's Alerts / Nursing Tasks / Nursing Summary) only when
  real API values exist — do not invent counts

ORDERS & SERVICES / WARD INVENTORY
  Same WardModulePageComponent (no OrdersV2 / independent ward stock)
  Shared CDK overlay menu (app-hms-action-menu) — last-row menus must not clip
  Mobile 390px: cards, not a squeezed table
  Inventory uses pharmacy inventory + ward requisitions

DUTY ROSTER
  Mental model: Date → Area → Shift → Coverage → Staff
  Tree-first on /ward/duty-roster; bootstrap GET /ward-billing/roster/bootstrap
    returns compact staff (id, name, role, employeeNo, department, photo, status)
    — not full User serialize / permission arrays / listUsers()
  Resource tree starts collapsed; rooms render only when a ward is expanded
  Assign Staff drawer opens immediately (OnPush). Eligible staff ranked in-memory
    with an assignment index (O(n+m)), cached, filtered, capped. switchMap cancels
    stale bootstrap loads. Staff checkbox never calls /roster/bulk/preview.
  Assign Nurse on Patient List is a separate bed-assignment modal using GET /ward/staff
    compact items. It does not load the Duty Roster tree.
  Open position pre-fills date/area/shift/role
  Bulk Assign: 4-step drawer; POST /ward-billing/roster/bulk/preview only on Review
  Conflict: 409 ROSTER_CONFLICT shown as Scheduling Conflict
  Documents: HmsDocumentService toolbar Help | Preview | Export PDF | Print (one Print)
  Tutorial: page-scoped Start Tutorial / Help (not TutorialV2)
  Staff without create/update see My Duty only


================================================================================
END OF CONTEXT — Hisaar360 Hospital Management System (Frontend)
================================================================================
