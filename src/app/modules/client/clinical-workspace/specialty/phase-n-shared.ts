/**
 * Shared field builders for Phase N specialty templates.
 * Re-exports from surgical-shared to avoid duplicating helpers.
 */
export {
  tri,
  text,
  textArea,
  select,
  dateField,
  yes,
  equals,
} from './surgical-shared';
